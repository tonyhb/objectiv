language: php
php:
  - 5.4
env:
  - KOHANA_ENV=TESTING
branches:
  only:
    - master
    - develop
services:
  - mongodb
before_install:
  - ./.travis-mongo-php.sh
  - git submodule update --init --recursive
  - cd application/tests
