// Generated by CoffeeScript 1.3.3
(function() {

  require(["modernizr", "app", "router", "views/menu", "collections/sites"], function(Modernizr, app, router, Menu, Sites) {
    app.Router = new router.Router();
    app.Menu = new Menu();
    app.Menu.render();
    app.Sites = new Sites();
    app.Sites.fetch();
    if (Modernizr.history) {
      Backbone.history.start({
        pushState: true,
        root: "/admin/"
      });
      $(document).on('click', 'a', function(e) {
        var href;
        href = e.target.getAttribute('href').replace('/admin/', '');
        app.Router.navigate(href, {
          trigger: true
        });
        e.preventDefault();
        return false;
      });
    } else {
      Backbone.history.start({
        root: "/admin/"
      });
    }
    return app.Router.navigate();
  });

}).call(this);
