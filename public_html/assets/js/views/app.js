// Generated by CoffeeScript 1.3.3
(function() {

  define(["app", "views/menu", "views/search"], function(app, MenuView, SearchView) {
    var AppView;
    return AppView = Backbone.View.extend({
      el: 'body',
      innerViews: {},
      initialize: function() {
        this.innerViews.MenuView = new MenuView();
        this.innerViews.SearchView = new SearchView();
        if (app.Sites.length === 1) {
          return app.Sites.setCurrentSite(app.Sites.models[0]);
        } else {

        }
      },
      render: function() {
        return _.each(this.innerViews, function(view, name) {
          return view.render();
        });
      }
    });
  });

}).call(this);
