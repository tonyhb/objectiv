// Generated by CoffeeScript 1.3.3
(function() {

  define(["app", "collections/themes", "text!templates/themes/view.html", "text!templates/themes/view.blank.html"], function(app, Themes, contentTemplate, blankSlateTemplate) {
    var ContentView;
    return ContentView = Backbone.View.extend({
      template: _.template(contentTemplate),
      blankSlateTemplate: _.template(blankSlateTemplate),
      initialize: function() {
        if (app.cache.Themes === void 0) {
          app.cache.Themes = new Themes();
          return app.cache.Themes.fetch();
        }
      },
      events: {},
      render: function() {
        if (app.cache.Themes.length === 0) {
          return this.$el.html(this.blankSlateTemplate({
            siteName: app.currentSite.get('name')
          }));
        } else {
          return this.$el.html(this.template());
        }
      }
    });
  });

}).call(this);
